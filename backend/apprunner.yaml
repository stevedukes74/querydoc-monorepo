version: 1.0
runtime: nodejs18
build:
  commands:
    build:
      - yarn install --frozen-lockfile
      - yarn build
run:
  runtime-version: 18
  command: node dist/server.js
  network:
    port: 8080
  env:
    - name: NODE_ENV
      value: production
